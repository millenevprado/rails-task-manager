<h1><%= @task.title %></h1>

<p><%= @task.details %></p>
<i class="<%= @task.completed ? 'fas fa-check-square' : 'far fa-square' %>"></i> <%= @task.completed ? 'This task is completed' : 'This task is not completed yet' %>


<%= link_to 'Edit task', edit_task_path(@task) %>
<%= link_to 'Back to list', tasks_path %>
